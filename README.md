# Emotion Detection Web App

A FastAPI-based emotion detection system with real-time WebSocket streaming and optional CSV batch analysis and SQLite history tracking integration
### Run locally
```bash
uvicorn app.main:app --reload
